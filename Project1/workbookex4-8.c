/*#include<stdio.h>
#pragma warning (disable:4996)
void main() {
	int count;
	int tmp;
	int total = 0;
	for (count = 1; count <= 5; count++) {
		printf("%d번째 정수값을 입력한 후 Enter키를 누르세요. ", count);
		scanf("%d", &tmp);
		total += tmp;
	}
	printf("읽은 정수의 충합은 %d 입니다. \n", total);
}*/